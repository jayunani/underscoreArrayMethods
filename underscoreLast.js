// _.last(array, [n]) 
// Returns the last element of an array. Passing n will return the last n elements of the 
// array.

// _.last([5, 4, 3, 2, 1]);
// => 1

var myLast = function(array, n) {
	var myArray = [];
	if(typeof n === "number") {
		
	}
	return array[array.length - 1]
}